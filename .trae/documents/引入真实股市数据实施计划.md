我已分析了现有代码结构，目前系统使用 `data_generator.py` 生成虚构数据。为了引入真实数据，我制定了以下实施计划。

我们将使用 **AkShare** 作为首选数据源，因为它是开源免费的，且能直接获取新浪/东方财富的实时行情，无需注册账号（相比之下 Tushare 需要 Token 且可能有积分门槛）。

### 第一步：创建工作计划文件
我将首先为您生成 `TodoList.md`，明确后续步骤。

**TodoList.md 内容预览：**
1.  **准备工作**
    *   [ ] (用户) 确认是否同意使用 AkShare (无需注册) 或其他数据源。
    *   [ ] 安装 `akshare` 库 (`pip install akshare`)。
2.  **代码实现**
    *   [ ] 创建 `data_fetcher.py`: 封装数据获取逻辑，适配现有 `DataFrame` 格式 (Date, Open, High, Low, Close, Volume)。
    *   [ ] 修改 `main.py`: 替换原本的 `generate_csi300_data` 调用，接入真实数据。
3.  **验证**
    *   [ ] 运行程序，检查生成的 K 线图是否与真实行情一致。

### 第二步：执行实施 (待确认后)
确认计划后，我将依次执行：
1.  创建 `TodoList.md`。
2.  安装依赖。
3.  编写 `data_fetcher.py` 并重构 `main.py`。

请确认是否继续？